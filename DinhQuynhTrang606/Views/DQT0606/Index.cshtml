@model IEnumerable<DinhQuynhTrang606.Models.DQT0606>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DQTName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DQTGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DQTName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DQTGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DQTId">Thêm</a> |
                <a asp-action="Details" asp-route-id="@item.DQTId">Sửa</a> |
                <a asp-action="Delete" asp-route-id="@item.DQTId">Xoá</a>
            </td>
        </tr>
}
    </tbody>
</table>
